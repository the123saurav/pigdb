#ifndef PIGDB_CORE_UTIL_H
#define PIGDB_CORE_UTIL_H

#include <cstdint>

namespace Pig {
    namespace Core {

        enum class CompressionType : uint8_t { NONE = 0 };

    } // namespace Core
} // namespace Pig

#endif